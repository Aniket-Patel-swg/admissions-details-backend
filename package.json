{
  "name": "admissions-backend-apis",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "npx serverless offline",
    "build": "esbuild src/server.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/server.mjs --sourcemap --external:axios --external:cheerio --external:express",
    "start": "node dist/server.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "^1.0.0",
    "express": "^5.2.1",
    "random-ipv4": "^1.0.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/express": "^5.0.6",
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.0",
    "serverless-offline": "^14.5.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
